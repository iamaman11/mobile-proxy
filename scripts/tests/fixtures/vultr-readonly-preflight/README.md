# Vultr read-only preflight fixtures

Negative coverage for the item-17 gate is generated in temporary directories by the Python tests;
no provider response, credential, SSH key or secret-derived fixture is stored in Git.
