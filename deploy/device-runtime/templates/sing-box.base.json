{
  "log": {
    "level": "warn"
  },
  "inbounds": [
    {
      "type": "mixed",
      "listen": "{{SING_BOX_LISTEN_HOST}}",
      "listen_port": 1080,
      "users": [
        {
          "username": "{{RELAY_USER}}",
          "password": "{{RELAY_PASSWORD}}"
        }
      ]
    },
    {
      "type": "socks",
      "listen": "{{SING_BOX_LISTEN_HOST}}",
      "listen_port": 1081,
      "users": [
        {
          "username": "{{RELAY_USER}}",
          "password": "{{RELAY_PASSWORD}}"
        }
      ]
    },
    {
      "type": "http",
      "listen": "{{SING_BOX_LISTEN_HOST}}",
      "listen_port": 3128,
      "users": [
        {
          "username": "{{RELAY_USER}}",
          "password": "{{RELAY_PASSWORD}}"
        }
      ]
    }
  ],
  "outbounds": [
    {
      "type": "direct"
    }
  ],
  "route": {
    "default_domain_resolver": "dns-cf"
  },
  "dns": {
    "strategy": "ipv4_only",
    "servers": [
      {
        "type": "udp",
        "tag": "dns-cf",
        "server": "1.1.1.1",
        "server_port": 53
      },
      {
        "type": "udp",
        "tag": "dns-google",
        "server": "8.8.8.8",
        "server_port": 53
      }
    ],
    "final": "dns-cf"
  }
}
