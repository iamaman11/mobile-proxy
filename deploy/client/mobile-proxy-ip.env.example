# Copy to ~/.config/mobile-proxy/client.env, replace the placeholders and chmod 600.
# The rotation token is scoped to status and IP rotation only.
MOBILE_PROXY_ROTATION_TOKEN='<rotation token>'
MOBILE_PROXY_REVERSE_TUNNEL_CERT_DER_B64='<pinned relay certificate>'
